# Stage 1, week 1 (this delivery) -- schema + chain-of-custody
blake3>=0.4.1
pytest>=8.0.0

# Stage 1, weeks 2-3 -- added as those modules are built
pymavlink>=2.4.40       # ArduPilot/PX4 .bin and .tlog parsing
pyulog>=1.2.4           # PX4 .ulg ULog binary parsing
construct>=2.10          # binary format definitions for custom parsers
cryptography>=42.0       # any future encrypted-container handling
folium>=0.16             # interactive flight path maps
simplekml>=1.3           # KML export
geopandas>=0.14          # geospatial analysis
reportlab>=4.1           # PDF report generation
jinja2>=3.1              # HTML report templating
typer>=0.12               # CLI entrypoint
fastapi>=0.111            # Phase 2 API layer
uvicorn>=0.29
